/**
 * Public GitHub default. The VDS production overlay replaces this file before
 * build with the real legal-contact address; never commit that replacement.
 */
export const publicContactEmail = "your-email@example.com";
